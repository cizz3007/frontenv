import styled from 'styled-components'
import {fontWeightNormal, GrayBlueColor, warmGrayColor} from '@global/styleVariable'

export const StyledHotelAddress = styled.address`
  color:${GrayBlueColor};
  font-size:1.2rem;
  line-height 1.8rem;
  font-weight:${fontWeightNormal};
`
