import styled,{css} from 'styled-components'
import {blackColor} from '@global/styleVariable'

export const DefaultModalParagraphStyle = styled.p`
     margin: 2.0rem auto;
     font-size: 1.4rem;
     color: ${blackColor};
`
